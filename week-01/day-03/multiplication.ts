'use strict';
export {};

let number: number = 9
let multiplier: number = 1

for (number; multiplier <= 10; multiplier++) {

console.log(multiplier + " * " + number + " = " + multiplier*number);
    
}
